version: 0.0
os: linux
files:
  - source: index.php
    destination: /var/www/html/
hooks:
   Applicationstart:
     - location: scripts/start_server.sh
       runas: ec2-user
   ApplicationStop:
     - location: scripts/stop_server.sh
       timeout: 300
       runas: ec2-user
